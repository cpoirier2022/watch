type Widget @model @auth(rules: [{allow: public}]) {
  id: ID!
  wid: String
  name: String
  src: String
}

type Fox @model @auth(rules: [{allow: public}]) {
  id: ID!
  fid: String
  name: String
  src: String
  user: ID
}
 